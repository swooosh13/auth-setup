# STATIC CONFIGURATION
log:
  level: INFO

api:
  insecure: true
  dashboard: true

entrypoints:
  web:
    adress: "80"

providers:
  docker:
    entrypoint: "unix:///var/run/docker.sock"
    exposedByDefault: false